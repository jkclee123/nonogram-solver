package solver

import "context"

// WorkerPool is a placeholder for the pool that consumes WorkItems
type WorkerPool struct{
	// stub fields for now
}

// Start starts the worker pool (stub)
func (p *WorkerPool) Start(ctx context.Context) {}

// Stop stops the worker pool (stub)
func (p *WorkerPool) Stop() {}
